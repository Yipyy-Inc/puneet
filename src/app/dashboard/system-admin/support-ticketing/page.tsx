import { SupportTicketing } from "@/components/system-admin/SupportTicketing";

export default function SupportTicketingPage() {
  return (
    <div className="bg-gradient-mesh bg-background min-h-screen flex-1 p-6 lg:p-8">
      <SupportTicketing />
    </div>
  );
}

import { GlobalSettings } from "@/components/system-admin/GlobalSettings";

export default function GlobalSettingsPage() {
  return (
    <div className="bg-gradient-mesh bg-background min-h-screen flex-1 p-6 lg:p-8">
      <GlobalSettings />
    </div>
  );
}

import { DataManagement } from "@/components/system-admin/DataManagement";

export default function DataManagementPage() {
  return (
    <div className="flex-1 p-6 lg:p-8 bg-background bg-gradient-mesh min-h-screen">
      <DataManagement />
    </div>
  );
}

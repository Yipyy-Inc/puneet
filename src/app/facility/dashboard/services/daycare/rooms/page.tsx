import { DaycareAreasClient } from "@/components/rooms/DaycareAreasClient";

// No facility to name — see the boarding rooms page beside this one.
export default function DaycareRoomsPage() {
  return <DaycareAreasClient />;
}
